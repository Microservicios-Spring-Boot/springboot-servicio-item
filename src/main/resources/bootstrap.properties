#1
spring.profiles.active=dev

#spring.cloud.config.uri=http://localhost:8888
management.endpoints.web.exposure.include=*
spring.config.import=optional:configserver:http//localhost:8888

# 1.- Perfil de Spring: Profile que se desee cargar desde la carpeta resources. Este atributo 
#     no es obligatorio.


#En cuanto a la sobrescritura de propiedades, se aplica la siguiente regla:
#Las propiedades definidas en bootstrap.properties tienen la máxima prioridad y sobrescriben cualquier otra propiedad en cualquier otro archivo de configuración.
#Las propiedades definidas en archivos específicos del perfil activo (application-dev.properties, application-prod.properties) sobrescriben las propiedades comunes definidas en application.properties (si hay alguna coincidencia).
#Si estás utilizando Spring Cloud Config Server, las propiedades cargadas desde el servidor de configuración centralizada se combinan con las propiedades locales y pueden sobrescribir las propiedades existentes según su orden de carga.
#En resumen, las propiedades definidas en bootstrap.properties tienen la máxima prioridad, seguidas de las propiedades específicas del perfil activo y las propiedades comunes en application.properties. Las propiedades cargadas desde el servidor de configuración centralizada pueden sobrescribir las propiedades locales según su orden de carga.